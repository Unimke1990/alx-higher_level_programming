#!/usr/bin/node

let myNum = process.argv[2];
let nums = parseInt(myNum);

if (isNaN(nums)){
	console.log('Missing number of occurrences')}
let a = 0;

while (a < nums) {console.log('C is fun')
	a++;
}
